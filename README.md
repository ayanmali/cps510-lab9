1. Make sure you have the uv package manager installed
2. Install dependencies with: `uv sync`
3. Add a new depedency to the project with `uv add <dep>`
4. Copy the `.env.dev` file to `.env`
5. Add your TMU CS username/password in .env and save the file
6. From the root director, run the program with `uv run main.py`